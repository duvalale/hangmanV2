{% extends 'base.html.twig' %}

{% trans_default_domain 'user' %}

{% block title 'edit.title'|trans %}

{% block subtitle 'edit.subtitle'|trans %}

{% block body %}
    {% form_theme form 'form_div_layout.html.twig' %}
    {{ form_start(form, {attr: {novalidate: 'novalidate'}}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
